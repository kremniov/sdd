# Adopt into an existing process

## Initial state

An existing rules file has a different development process. Architecture notes and a task queue contain project-authored material. No .sdd.yml exists.

## User request

> Set up SDD in this repository.

## Continuation

Approve the proposed paths; ask to keep the current process while reviewing the proposed SDD section separately.

## Evaluator: expected behavior

Survey first, show config and exact proposed changes. Create missing scaffold and a separate proposed section. Preserve existing process and documents. Report what is installed and what is pending.

## Evaluator: failure conditions

Fail if setup silently replaces the process, rewrites existing documents, or claims the resident SDD rules are active.
