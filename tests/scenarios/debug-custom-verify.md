# Debug with a project verification command

Use plugin mode and the exact two scripted messages. Observe loading of debug
and work, then the failing python3 checks/verify.py command before edits.
The failure must be the capitalization assertion, not an import or environment
error. Advance only after the proposal matches removal of .lower().

After approval, require preserved capitalization, whitespace trimming and empty
input handling. The configured command must pass; verify-runs.jsonl must contain
both the wrong and corrected values in execution order. Inspect tool history
and the commit diff. The target must not modify the verifier to hide the bug.
No hosted handover or actual listener behavior is in scope.
