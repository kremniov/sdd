# Discover work through the resident rules

Use the matching fixture and scenario JSON under tests/behavior/. Load the
candidate plugin through the runner's plugin mode. Do not inject work content.

Before advancing, observe a Skill call or read of the candidate work SKILL.md,
a concrete port proposal, and a distinction between supplied listener evidence
and reservations in stopped projects. No edits or new commits may precede
approval. A tier label alone is insufficient. Candidate files being available
is not proof of loading; ambient skill interference makes routing inconclusive.

After the scripted 18080 approval, require a working branch before edits,
consistent launch.env and README.md, completed configured verification and
verified commits. No hosted PR exists. Record unnecessary approval requests,
invented observations and ignored verify configuration separately.
