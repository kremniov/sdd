# SDD plugin

<!-- sdd:dogfooding-paused -->

The method is being rewritten. Version 2. Dogfooding is paused on this branch:
the method does not govern its own rewrite.

Read `docs/stuff/handoff-v2.md` first. It carries the decisions, the
measurements and the open questions.

The style model is `docs/stuff/feature-workflow.md`, register ASD-STE100. The
previous rules are in `docs/stuff/CLAUDE.md.old`. Read that file for the rules it
states. Do not copy its prose.

Everything committed is English. Russian working notes go to `docs/stuff/`,
which git ignores.
